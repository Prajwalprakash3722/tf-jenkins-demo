---
- name: Install Nginx & fluent-bit
  hosts: nodes
  remote_user: ubuntu
  gather_facts: yes
  become: yes
  vars:
    fluentbit_codename: "{{ ansible_distribution_release }}"
    label_value:  "{{inventory_hostname }}"
  roles:
  - fluent-bit
  - nginx
